{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The single desktop recycle bin window",
  "windows": ["trash-bin"],
  "permissions": ["core:default"]
}
